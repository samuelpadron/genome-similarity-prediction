from setuptools import setup, find_packages

setup(
    name='hyena-dna-genome-similarity',
    version='0.1',
    description='Fork of the hyena-dna repository for predecting local genome similarity in plants',
    author='Samuel Padron',
    packages=find_packages(),
)
